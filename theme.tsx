import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#264768",
      light: "#417bb4",
      dark: "#0a121b",
    },
  },
});

export default theme;
